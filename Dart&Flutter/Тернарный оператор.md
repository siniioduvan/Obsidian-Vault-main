#Flutter #Dart #УсловныйОператор
В определённых ситуациях `if-else` избыточен, тогда на помощь приходит тернарный оператор:
```dart
if (10 % 2 == 0){
	result = true;
}else{
	result = false;
}

//можно записать короче
result 10 % 2 == 0 ? true : false
```